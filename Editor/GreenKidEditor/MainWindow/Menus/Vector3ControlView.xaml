<UserControl x:Class="GreenKidEditor.MainWindow.Menus.Vector3ControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Style="{ StaticResource LabelHeaderStyle }" Content="{ Binding Path=Header, Mode=OneTime }"/>

        <TextBox Grid.Column="1" Style="{ StaticResource TextBoxCenterValue }" Text="{ Binding Path=X, Mode=TwoWay }"/>
        <TextBox Grid.Column="2" Style="{ StaticResource TextBoxCenterValue }" Text="{ Binding Path=Y, Mode=TwoWay }" Margin="2, 0, 0, 0"/>
        <TextBox Grid.Column="3" Style="{ StaticResource TextBoxCenterValue }" Text="{ Binding Path=Z, Mode=TwoWay }" Margin="2, 0, 0, 0"/>
    </Grid>
</UserControl>