<UserControl x:Class="GreenKidCreator.Panels.Transform.TransformPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
             xmlns:transform="clr-namespace:GreenKidCreator.Panels.Transform"
             xmlns:tree="clr-namespace:GreenKidCreator.Tabs.Tree">

    <UserControl.Resources>
        <DataTemplate DataType="{ x:Type transform:TransformPanelViewModel }">
            <Grid Margin="0, 5, 0, 0">
                <Label Content="Test"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Border Style="{ StaticResource BorderStyle }" Margin="0, 2, 0, 0">
        <Expander IsExpanded="True">
            <Expander.Header>
                <Label Style="{ StaticResource ExpanderHeaderStyle }" Content="Transform"/>
            </Expander.Header>
            
            <!-- Content -->
            <ContentPresenter Content="{ Binding Path=(tree:BasicTreeItemViewModel.Transform), Mode=OneWay }"/>
        </Expander>
    </Border>
</UserControl>