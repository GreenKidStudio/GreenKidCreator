<UserControl x:Class="GreenKidCreator.Tabs.Tree.BasicTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:command="http://www.galasoft.ch/mvvmlight" mc:Ignorable="d"
             xmlns:tree="clr-namespace:GreenKidCreator.Tabs.Tree">

    <TreeView ItemsSource="{ Binding Path=(tree:BasicTreeViewModel.Items), Mode=OneWay, IsAsync=True }">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="SelectedItemChanged">
                <command:EventToCommand Command="{ Binding Path=(tree:BasicTreeViewModel.SelectedItemCmd), Mode=OneTime }" PassEventArgsToCommand="True"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>

        <TreeView.Resources>
            <HierarchicalDataTemplate DataType="{ x:Type tree:BasicTreeItemCollectionViewModel }">
                <TextBlock Text="Test" Padding="2, 0"/>
            </HierarchicalDataTemplate>

            <DataTemplate DataType="{ x:Type tree:BasicTreeItemViewModel }">
                <TextBlock Text="Test" Padding="2, 0"/>
            </DataTemplate>
        </TreeView.Resources>
    </TreeView>
</UserControl>