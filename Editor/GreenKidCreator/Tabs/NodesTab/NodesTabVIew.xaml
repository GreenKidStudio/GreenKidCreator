<UserControl x:Class="GreenKidCreator.Tabs.NodesTab.NodesTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:tree="clr-namespace:GreenKidCreator.Tabs.Tree"
             xmlns:panels="clr-namespace:GreenKidCreator.Panels"
             xmlns:menus="clr-namespace:GreenKidCreator.Menus" mc:Ignorable="d"
             
             DataContext="{ Binding Source={ StaticResource ViewModelLocator }, Path=MainWindow.NodesTab, Mode=OneTime }">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <tree:BasicTreeView Grid.Row="0" DataContext="{ Binding Path=ItemsTree, Mode=OneWay }"/>
        
        <panels:NodePanelView Grid.Row="2"/>
        <panels:TransformPanelView Grid.Row="3"/>

        <menus:SearchBarView Grid.Row="1"/>
        <menus:AddItemBarView Grid.Row="4"/>
    </Grid>
</UserControl>